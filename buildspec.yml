version: 0.1

phases:
  build:
    commands:
      - echo Build started on `date`
      - mvn package
    post_build:
      - echo Build completed on `date`
artifacts:
  files: target/lambda-java-example-1.0-SNAPSHOT.jar
      
